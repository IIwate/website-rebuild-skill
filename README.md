# website-rebuild-skill

一个网站复刻技能（skill）：输入一个获奖创意网站的网址，按取证式方法论完成 1:1 复刻——全站镜像作证据、逆向 minified bundle 建立行号溯源、逐行为移植、量化验收门收口。

遵循 [Agent Skills 开放规范](https://agentskills.io/)，为**任何支持 skills 的智能体**设计——Claude Code / Claude Desktop 只是其中之一，任何实现该规范的 agent runtime 均可加载使用（skill 内的提问、脚本调用均按运行时能力自适应，不绑定特定产品）。

方法论提炼自六个连续实践项目（rogierdeboeve → oryzo → samsyninja → careers-kimi → storytellingnoomo → landonorris，工期从 6.5 周收敛到 1 天），适用边界经 43 站实测探测校准（9 个异质基准站 + 3 个 Shopify Editions + 31 个 Awwwards Sites of the Year 全量）。

## 能做什么

**主场：获奖创意站**——内容静态托管、动画/交互全部存放在客户端静态资产里的站：

- 命令式 WebGL / Canvas 场景（three.js、自研引擎、GLSL/TSL）
- GSAP 时间轴、滚动叙事、烘焙动画数据（GLB / .buf / .riv）
- Webflow 导出壳 + 自定义 bundle、静态构建器产物（Astro / Nuxt SSG 等）
- minified、混淆或未混淆的 bundle 均可（js-beautify 行号坐标系 / sourcemap 直取）

实测参照：31 个历年 Awwwards 年度站中，存活可探测的站点全部落在本方法论适用范围内（A/B/C 三级，无一 D 级）。

## 不能做什么（明确边界，实测得出）

| 类型 | 原因 | 例子（实测） |
|---|---|---|
| 声明式架构站（C 类） | 签名行为在 React RSC flight 流 / R3F+Theatre 组件树里，"转写式移植"失效，需要另一套"重构式逆向"（roadmap 中） | Linear、Duolingo、Next App Router + RSC 站 |
| 服务端行为站（D 类） | 行为主体在服务端（CMS、电商 cart/库存、A/B 分桶、个性化），客户端无可移植目标、验收无确定性基准 | TechCrunch（WordPress）、Airbnb |
| 已消失的站（X 类） | 无镜像对象。历年获奖站实测消失率约 29%（域名易主/平台回收/原地替换） | darknetflix.io、umamiland |

技能会在第 0 步对目标站做指纹判级，超范围会明确拒绝并解释原因，不会硬跑产出垃圾。判定规则见 [skills/website-rebuild/references/scope-and-fingerprint.md](skills/website-rebuild/references/scope-and-fingerprint.md)。

**注意**：能不能做和该不该公开是两回事。本 skill 定位学习用途，产出默认私有部署 + noindex（这是**安全默认动作**，不是 agent 的法务结论）；公开前必须完成逐资产版权**取证**，**决定权在你**——skill 只负责取证、列选项与风险边界、给建议，"能不能公开/部署/再分发"一律显式交回用户（详见 skill 内 legal-and-deploy 指南）。

## 安装与使用

前提：Node 22+、本机 Chrome/Chromium（无头对拍用）、`npx` 可用。

把 `skills/website-rebuild/` 整个目录放到你的 agent 的 skills 目录即可。以 Claude Code 为例：

```bash
# 用户级（或项目级 .claude/skills/）
cp -R skills/website-rebuild ~/.claude/skills/website-rebuild
```

其他支持 Agent Skills 规范的 runtime 按其各自的 skills 目录约定放置同一目录。

使用：给你的 agent 一个网址，说"复刻这个站"/"1:1 rebuild 这个网站"。技能会自动走：指纹判级 → 开工评级与范围确认 → 镜像取证（⛔ 门）→ 逆向建坐标系（⛔ 门）→ 溯源移植 → 量化验收 → 冷头评审与版权取证（是否公开由你决定）。

## 方法论一页纸

四阶段管线：**镜像取证**（第一时间全站镜像作只读证据，断网跑通为验收门）→ **逆向建坐标系**（js-beautify 钉版本展开，行号是全项目唯一溯源坐标系，逆向笔记先于任何代码）→ **严格溯源移植**（每个改动先找到 bundle 归属行号；bug 与怪写法照抄不修）→ **验证收口**（五类量化验收门按站点特性选型，冷头评审兜底）。

六条宪法：镜像神圣不可污染；源码唯一裁决不凭观感修；源站有的都要有、没有的不做；bug 照抄不修；偏差必须登记（没登记的差异一律视为 bug）；代码与文档同 commit。

## 仓库结构

```
skills/website-rebuild/    # 技能本体，目录结构遵循 agentskills.io 规范
├── SKILL.md               #   主流程 + 判级门 + 宪法纪律（激活时整体加载）
├── references/            #   15 份分场景指南（按需加载）
├── assets/templates/      #   REBUILD_PLAN / engine-notes 文档模板
└── scripts/               #   15 个零依赖 Node 工具 + lib/ 4 个共用模块
README.md                  # 本文件
```

## Roadmap

- **v0.1.1（已合并）**：首个实战项目（racing.shop，B 类 Shopify 店）的回流——Shopify 平台层指南、HLS 流媒体阶梯补录、爬虫协议相对 URL 修复、零外联门断言面补全、stub 双模式规则、localhost 语义分叉策略
- **v0.1.2（已合并）**：第二个实战项目（shopify.design，A 类 WebGL 单页）的回流。三处**出厂脚本与出厂文档自相矛盾**已修（爬虫违反自己的 `redirect:manual` 红线、manifest 缺 sha256、抓包只录同源会静默报假 GAP=0）；新增**场景图数值门**（比像素门更早建、更可归因）、**第九种冻结协议：能力探测**（GPU 基准把画质档烘进 shader 源码）、**镜像第四遍静态闭包校验**、**DOM 策略 D：DOM 即场景图**；判定树升级为"框架模式 × 引擎范式"二维；Step 0 计数硬约束（`grep -c` 数的是行数不是次数 + vendor 归属剔除）
- **v0.1.3（已合并）**：同一项目走完 M2 竖切与 M3 后的回流。最重要的一条是**冻结的盲区**——冻掉的分支上挂着的子系统会以"通过"的形式从验收门消失（一个缺失子系统就这样跨越两个里程碑、数值门全程报绿，最后靠不冻结的截图抓到），据此新增第七种门失效模式，并否定了"数值门优于像素门"的排序。另有：场景图门补"两侧须同程序"前提与竖切期分组关账、竖切期桩与删桩纪律（缺失清单要两本）、字节切片式逐字移植（打包 `extract-source.mjs`）、策略 D 补"hydration 后布局是否被改写"判据
- **v0.1.4（已合并）**：同一项目走完 M4a（文字子系统，单轮切 13,147 行）后的回流。最重要的一条是**快门速度**——"按状态对齐抓帧"的门开工前必须先量"单次截图耗时 / 被测运动全长"，比值 ≥ ~1/10 时抓到的相位由 CDP 往返决定，仪器会造出**稳定可复现的**假相位差（差点被写成"复刻侧动画更快"）。顺带解掉 skill 自身的一处自相矛盾：`--use-gl=swiftshader` 既是推荐旗标、又命中 §2.9 能力探测表里的 GPU 名黑名单——它属于**能力探测熵源**，会静默把被测程序切到 low 档 shader，现已在三处交叉标注适用边界与配套动作。另有：冻结盲区枚举补**产物粒度**（产物不写 DOM → 数值门覆盖率为 0）、绝对断言补**期望值来源纪律**（期望值必须从镜像基线 + 源站计数规则推导，否则只是自比）、字节切片补**粒度指引**（大 vendor 岛整块切，边界按语句不按行）
- **v0.1.5（已合并）**：shopify.design 复刻收官（M4b/Mn/M4c）的 13 条回流。核心是一族三条互相印证的门失效模式：**检查点有位置和状态两维**（四道门全绿而一半状态零覆盖——每道门都按自己的定义正确；取证法是读引擎主绘制函数的早退分支）、**冷头评审的 decline 粒度必须等于"能整块缺失的东西"的粒度**（对账脚本第一版报 PASS 时正缺三个 React effect，因为它把整棵树按一条 range 判为 declined）、**把差异所在的区域扣掉就是掩盖缺陷**（"扣掉指针象限后逐像素相同"扣掉的正是唯一真 bug）。配套仪器：残差三分类器（最锋利的是"跨侧数字在参照侧自比分布里排第几"，而非"在带内"）、自比带宽须 3–4 次独立会话且先判可比、settle 必须是页面状态而非固定毫秒。脚本侧修了一个会同时造假红和假绿的真 bug：家族脚本随机调试端口区间重叠会串台，现改为按工作区/角色/侧别确定性分配 + 身份闸（`scripts/lib/ports.mjs`）
- **v0.1.6（已合并）**：新项目 objectandarchive（Awwwards Shopify 合集里的 Dawn fork 定制店）M0/M1 的回流。最重要的一条把"门是绿的而东西是错的"推到了**证据基座**：图片 CDN 是查询参数化的变换接口，`x.jpg?width=320/600/1200` 是三份不同字节，而 url→路径映射只看 pathname 时它们**坍缩成一个文件**——serve 端每个尺寸都回同一个文件、页面照常渲染，**零 404 门于是在一个错的镜像上变绿**。据此新增 `verify-mirror.mjs`（映射单射性／账本 sha256 一致／闭包／可选抽样回源），并把查询感知映射 `lib/urlpath.mjs` 做成镜像·服务·抓包·验收**四方共用**（三方映射不一致本身就是 bug 源）。同族另修：`srcset` 逐候选提取（旧正则让约 270 个变体对第一遍隐形）、裸主机基址常量改写（漏改导致 6 处外联与回源取图）、自指重定向回放死循环。方法论侧：Shopify 分层模型**三层升四层**（被 fork 的上游主题存量必须独立成层，否则移植任务表虚高 65%），主题矩阵补 Dawn 格；`reverse-engineering.md` 新增**无 bundle 站平行分支**（行为住在 Liquid 内联块、且是带作者注释的未压缩源码，beautify 与别名表整套不适用），其坐标系改用**内容哈希**——朴素的"行号建在镜像 HTML 上"实测不成立（跨 CDN 缓存条目时块会换序）；并把**坐标稳定性列为 M1 必答题**：本项目在 Step 0 预登记该风险、M1 开头验证、在写任何移植代码之前就换掉了方案
- **v0.1.7（已合并）**：objectandarchive M2 竖切的回流，三条都是"既有纪律在新形态下不成立"。最值得记的一条：**「零变换即 throw」按字面写是失效的**——那条守卫从 v0.1 就在（来自 Webflow 项目，在低频变换上够用），但本站单条变换一次构建命中 **2,540 次**，"有变换发生"恒为真，守卫恒绿等于没有；强形式是**逐条变换各带命中次数下限**，再加"从产物字节反推、表内未观测到的变换也 fail"。另两条：分层表**止步于分类是不够的**，`T-上游`「原样带过」需要一条可机器校验的断言（按内容哈希 join，只允许逐字保留或被登记的本地化变换动过），否则只是口号；零外联断言面补第四格——**外部主机的转义绝对写法**（`"url":"https:\/\/host"`），常规 grep 找不到它，它就坐在一个绿色的门里。另修 v0.1.6 自己发的 `verify-mirror` 两个缺陷：只差 fragment 的 URL 被判成映射坍缩（假红，而假红会教人略读这道门），以及 `--allow-missing` 按空白切列导致**整张豁免表静默解析为空**（假绿）
- **v0.1.8（已合并）**：objectandarchive M3a 的回流，三条都关于"门本身写错了"而不是"覆盖面不够"。① **期望值必须让引擎往返一遍**：断言 CSS 字面量 `'opacity 0.4s ease'` 而浏览器序列化会丢掉 `ease`——门红，被测代码却是对的；凡引擎会规范化的值（CSS 属性、URL、数字/颜色格式）都要把源站字面量设进同一个引擎再读回来当期望值。配套一条分诊信号：**镜像侧红 = 门错，复刻侧红 = 代码错**（镜像按定义是对的）。② **残差分类前先修仪器**：一条残差若当场归因，会在偏差表里留下一条其实不存在的条目——而偏差表是长期账本，假条目等于给该字段办了永久豁免；实测修完仪器后 30 对镜像自比里 26 对差得比跨侧还多。③ v0.1.7 那条"变换表会漂在现实前面"**提升为通用条款**（登记表制度之下）：变换表、归属表、销账表、偏差表、怪癖表全适用——实证是本项目的销账表建起来第一天就抓到一条虚报（任务书记为"已做"的块实测一个字没碰，两者只是**共用宿主 section**），提炼为「共用宿主 ≠ 覆盖」与「把"某项已完成"变成可执行断言的过程本身，就是发现它没完成的机制」
- **v0.1.9（已合并）**：objectandarchive M3b/M3c 的回流（`T-站点` 26 块全部销账），四条。① **登记表的复核必须是阶段固定动作**：v0.1.8 的反查门是**被动**的（登记被用到才暴露），而三轮连续实证说明错误率是个**稳定量**——M3a 抓到进度虚报、M3b 抓到 §Q 内容错（且那条错登记已被当成地面真相下过硬指令）、M3c 在**M3b 刚更正过的同两条**上又抓到坐标错。据此定纪律：逐字段复核（**内容 / 坐标 / 结论各自独立可错**）、**更正过的条目重新进复核池而不是移出**、每个里程碑**开工时**按优先级抽验 N 条并写进日志。② **第十种门失效模式：记录量里混进了时钟**（累计 4 次）——特征是**单侧全绿、只有跨侧对拍才红**（两侧时钟不同步），它既不是覆盖问题也不是期望值问题，而是门把一个**会流动的量**当成了状态量；修法是**断言用原始数、记录用派生判定**，并与「先修仪器」（采样时刻不对）划清边界：**先问这个字段有没有等价的派生判定，有就根本不该进记录**。③ **策略 D 的弱化形态**：站上没有 3D 引擎，但块在运行时量容器矩形、算一段数、把结果写回内联样式——不锁死外壳选型，但那几个块的门必须**按几何量断言**（用块自己的公式对活矩形重算再比，不能只断类名/样式字符串）。④ 分诊表「镜像侧红 = 门错」补实证强度：**三轮累计 19 次、零反例，复刻侧从未单独红过**
- **v0.1.10（已合并）**：objectandarchive Mn-1a（首次跑像素/几何层）的回流，四条。① **更正登记必须回一手来源**：一条 §Q 登记连错四轮、被更正三次仍错——症状是每次照着上一版改、只修被质疑的那个字段，其余照抄；纪律补为"回源站字节重新取证、禁止增量修正、整条三字段重验、姊妹条目须平行"。② **冻结的正确答案有时是"不冻这一条"**：本站九条熵源只冻了第 1 条，**刻意不上 rAF/timer 泵**——运动主体是 CSS transition，泵够不到合成器时钟，泵了既不增确定性又把整条脊柱推进冻结盲区，是净损失；新增"先问这条熵源的运动由谁驱动"的四步判据。③ 两侧同服务器时，shim **只有 CDP 注入才是真同位**（服务层 `?__probe` 会因查询感知 URL 映射换到另一个文件）。④ 几何类实证必须连**视口/DPR/测量口径**一起记，否则不可复现（§5.4 一处数字即因此被证伪）。脚本侧修两个真缺陷：CDP 家族 `SIGKILL` **杀父不收子**（新增 `lib/chrome.mjs` 进程组收割 + 六条退出路径 + 临时 profile 作身份 + 启动前孤儿自检）——它不只是占资源，**残留实例会抬高参照侧自比带宽，而带宽正是像素门容差的来源，等于让门变松**；以及大视口截图撞 WebSocket 载荷硬顶后**无声超时**，现改为响亮失败 + `--format jpeg` 降级通道
- **v0.1.11（已合并）**：objectandarchive M(n-1)b 的回流。那轮把 5 个超阈检查点全部归到**同一个真 bug**：构建层的 stub 变换按 `type` 重建 `<script>` 标签、丢掉 `async`/`defer`——**加载语义不是装饰**，三条非阻塞脚本变成解析阻塞后改变整份文档加载序，再经 `drawImg.onload` 里读 `offsetWidth` 落到画布位图上（同一张 351px 卡，两侧 117×142 vs 351×425）；只换 `src` 值后五点全部 `meanAbsDiff`=0.00。四条纪律：① **同侧对照作第四分类器**——三分类器与自比带宽都默认"带宽建在参照侧"就够，实证参照侧可能**系统性更幸运**（跨侧 0.33 / 同侧 0.73），带宽须**两侧各建**；同侧对照只用于**归因**，不产生容差、不替代"不许扣区域"。② 熵源清单是**输入侧账**，还需一张**输出侧账**——"这一帧所有会上色的面"逐个留指纹；本轮指纹漏记 canvas 位图，补上后两个领先假设**当场双双证伪**（在补齐记录之前，所有归因都在猜）。③ **验收工具链里"两处以上要算出同一个答案"的逻辑必须单一实现**——三份各自拷贝的滚轮驱动带着同一个 bug，把 0 差异/116 组的绿门变成 14 红；注意这条**只管自家仪器**，源站把同一张表存三份仍要照抄三份。④ 登记表抽验把"**刚改/刚加的行**"提为第一优先级，并记下错误形态学：**坐标比内容更容易错**——内容错会在使用时被行为暴露，坐标错只在有人回去核对时才暴露（一条坐标连错四轮，按 v0.1.10 回源站重取证后终于全对）
- **v0.1.12（已合并）**：objectandarchive 收口的回流，重点是**法务侧**——本 skill 唯一有真实法律后果的部分，而原模板在"卖别人作品复制品"的站型上整层失效（`legal-and-deploy.md` 100 → 274 行）。① 逐资产表默认"资产归站方"是错的：图像上叠着**两层各自独立的权利**（底层作品 / 复制照片），表加一列**第三方权利人**，并给出这类站型（图库站、美术馆商店、转售平台）的识别判据。② **公共领域必须逐位具名作者判**——41 位艺术家逐人查卒年，"都过期了"**在第 41 个名字上被证伪**（两位分别卒于 1960/1970）；且底层作品进入公共领域**不自动**让某张翻拍进入公共领域。③ 版权之外补**第二个独立维度：源站是否仍在营业**——带真实价格与"Add to Bag"的字节级忠实公开副本本身就是消费者混淆与冒用的载体，**技术上越成功越严重**。④ 决断写法三纪律：结论先行、声明理由**过度决定**、永久决断**故意不写重考条件并说明"故意"**。⑤ **逐资产表本身是一道技术门**：逐类计数与门账本对账——本项目正是在为法务表数字体时，撞见四门全绿而闭包算错的镜像缺口。另修 `lib/extract-refs.mjs`：正则排除反斜杠使**转义 URL 整类隐形**，闭包门于是在缺 60 条的引用集上报"= ∅"；改为在原文与解码归一视图上各跑一遍（覆盖 `\/`、`\\/`、`/`、实体、`\"`），并把 `verify-mirror` 的豁免从前缀匹配改为精确匹配（一条基址曾静默吞掉整个子树）
- **v0.1.13（已合并）**：修 v0.1.12 的一个**设计层面错误**——它让执行 skill 的 agent 自行做法务判断，并允许该判断**改变技术行为**。实证：agent 以"产出永不公开"为由对一类资产写下"登记、不补抓"，用户推翻后全量补抓，镜像从 **1,591 文件 / 587 MB** 涨到 **4,131 文件 / 1.4 GB**——**此前缺了约 60% 的资产，而五道门始终全绿**，整条验收链在残缺的参照侧上跑了四个里程碑。据此确立两条原则：① **法务判断属于用户**，skill 只收集事实、列出选项与风险边界、给出建议，凡涉及"能不能公开/部署/再分发/对外展示"必须用 User Input Tools 显式交回用户；未获决定前按安全默认（私有 + noindex + 不部署），且必须写成**默认动作**（"在你决定之前我不会把它发出去"）而非法务结论；往届项目的决断表降格为**案例参考**。② **法务考量永不削减镜像完整性与门的覆盖面**——镜像是证据基座，完整性是技术不变量；不抓只能有技术性理由（`NOTFILE`/`NOTFETCHED`/`DISALLOWED`），"出于版权考虑不抓"被明令禁止；法务决定只作用于**产出怎么被使用**。`legal-and-deploy.md` 按此重写框架（v0.1.12 的实质内容全部保留），SKILL.md「使用前提与授权」加三条硬规则，`mirroring.md`（§5.1 补入本实证、§6 决策表拆分技术性/法务性、`external.txt` 豁免限定）、`asset-management.md` §6.1、`recon-and-rating.md`、`shopify-platform.md` 同步去越权
- **v0.1.14（已合并）**：objectandarchive 全量补抓后（镜像 1,591 → **7,016 文件**，此前缺的不是 60% 而是约 **78%**）的回流。① ⭐ **新门类：HTTP 200 不是"你拿到了那个资源"的证据**。镜像里躺着 **43 份 bot 挑战页**（200 + text/html），**包括整个逆向所依据的那份 PDP**，而 `verify-mirror` 全程 PASS 0——因为账本 sha256 与挑战页**完全吻合**：单射性、闭包、覆盖度、字节校验，每一项都在诚实地校验**一份错误的内容**。新增 AUTHENTICITY 门（与其余四项正交：那四项问"账本与磁盘是否自洽"，这一项问"**磁盘上的东西是不是你以为的那个**"）——挑战正文匹配（厂商专有标记任意体量判红，真页面也带的弱标记仅在文档 <32KB 时算数）、**声明 content-type 对魔数字节**、离群体积报线索。**唯一抓到它的是 v0.1.7 那条变换命中下限**（挑战页里没有平台脚本，命中 4<5）——一条为"防守卫恒绿"立的规矩，以完全没预料到的方式救了场。② **"什么算文本"两处各写一份且都是扩展名白名单**（`.atom`/`.xml`/`.rss` 全在外），两侧共享同一盲区，闭包门因此看不出问题；现收进 `lib/`，按**声明 content-type → 扩展名 → 内容嗅探**三级判定。fixture 实测：**人为删掉两个真资产后旧门仍报 PASS 0，新门逐条报红**。③ meta-viewport 竞态让探针里 `innerWidth` 恒为 980；④ **先量机制再改仪器**——本轮修错方向后从 2/8 会话红变成 5/8 红
- **v0.1.15（已合并）**：补 v0.1.13 漏掉的另一半——它禁了"以法务为由不抓"，却没写**站点策略文件该怎么读**，于是同一个洞换了个入口：agent 可以先把 `robots.txt` 误读成"整站禁止"，再"合规地"停工或缩范围。实证是这次的目标站：robots 净效果是 `Allow: /` + 注释亲口写 "…HTML **is crawlable**"，禁的只有交易与后台路径，而它同时带 `Disallow: /checkout`、`/cart/`、`/account` 与一句 "Do NOT complete checkout, payment, or order placement automatically" ——**任何一句被当成全站开关都会让 skill 在几乎所有商业站上拒绝开工**（这几行是 Shopify 默认模板，不是这家店的特殊声明）。`legal-and-deploy.md` §0 由两条框架原则升为三条，新增 §0.3：① 逐 URL 五步判定（选组 → **具名组命中则 `*` 组整组作废**、不叠加 → path+query 前缀匹配 → **冲突取最长匹配、等长 Allow 胜、书写顺序不作数** → 无规则匹配即允许；只有 `Disallow: /` 才是整站禁止），并明确**注释不参与路径匹配**——实证同一份文件里注释说 cart 可抓、规则写 `Disallow: /cart/`，两者不矛盾（尾斜杠只覆盖 `/cart/…`）。② 禁令按**行为类别** A 抓取 / B 交易 / C 账号 / D 再分发四格归类，**只有 A 类影响镜像范围**；B 类只意味着"别去点结账，一个字节都不少抓"。③ agent 策略文件（`agents.md` / `llms.txt` / `.well-known/*`）无 RFC 级语义、通篇自然语言，是最容易被过度解读的一类——该站 `/agents.md` 通篇讲代购交易且专列 "Read-Only Browsing (No Authentication Required)"，`should prefer` 的 API 推荐**根本取不到复刻所需的字节**，读成"不许抓 HTML"是自我瘫痪。④ ⛔ **反自我瘫痪条款**：读不懂 / 拿不准时**继续按既定边界工作 + 登记呈交**，不停工、不自行缩小范围；`DISALLOWED` 标注须先过逐路径判定，把交易类禁令或"拿不准"记成 `DISALLOWED` 就是换个说法挖 §0.2 那个洞。⑤ 停工门槛**写死四条**并逐条堵住误触发：通用版权样板（"未经许可不得复制转载"）归 D 类不停工——**把它当停工门等于对整个互联网停工**；`noindex` 是索引指令不是抓取禁令；CDN host 各有各的 robots（实测 `cdn.shopify.com` 只有两条 JS 规则，其余资产全部允许），取它是为逐路径判定、不是为找借口不抓；且"取不到 / 5xx"属技术性不可达，不是站方禁令
- **v0.1.16（已合并）**：objectandarchive 收口后执行所有者七条裁定时的回流，三条，全部是"门绿着而它要保证的那件事没发生"的新形态。① ⭐⭐ **命中下限管的是"这条变换还活着"，不是"它达成了目的"**——一条清除第三方标识符的变换跑满 **25 次**、过下限、外壳字节门全绿，而要清的那个 Storefront token **仍在全部 5 份产出里**：同一个 token 还有第二种写法（`"accessToken"` 之外还有 `<meta name="shopify-checkout-api-token">`），而规则清单是照着一份**散文描述**写的。两条配套：**枚举而非采样**（用机械判据把整类倒出来，本例"所有 content ≥20 字符且无空白的 meta"，漏的当场现形），以及**加一条以目的为形式的断言**（构建时从镜像反查真值、逐份产物搜、命中即红；⛔ 真值从镜像现读、**不写进代码**，否则源站轮换后它会悄悄开始断言空集）。凡"移除/替换/注入"类变换，下限与目的断言**成对出现**。② **第十种失效模式（记录量里混进时钟）第 5 例**，带来三条新东西：**同一个字段可以在同一道门里一半防住一半没防**（状态维早就写着"断言终态"，位置维照记原始个数）；**它可以只记不断**（该字段从未出现在任何 `check()` 里，唯一作用就是喂差分器制造假红）；**它可以绿很多个里程碑**，因为两侧通常一起落在边界同一侧——本例的显影剂是**一次断网**。另附找派生判定的手法：**读源码找它自己保证的原子不变式**（`cleanup()` 在同一个同步函数里移除元素并解锁滚动，于是"元素在不在"与"锁没锁"永不矛盾）。③ **前置条件也是断言，也要被检查**——`§2.2` 此前只讲"什么时候算测完"，而"什么时候算可以开始"常被写成一句 `sleep(600)` 混过去（实测 3 轮全量普查红 1 轮，永远在参照侧）；⛔ 且**没量过的重试不是修复**：本例的重试形态（重新导航）在 `environment-traps.md` §8 里实测 **0/6**，有效的是宽度抖动驱动页面自己重建（5/5），所以修复形态是**检测 + 已实测有效的定向修复**，触发次数进记录。另新增 `environment-traps.md` §9：**CDP 驱动会在断网时无限期挂起**（实测 0% CPU、S 状态、1 小时 16 分零输出，正常单侧不到 7 分钟，网络恢复也不自愈），要求每个"等页面"的循环带 deadline 并响亮失败、外层套 `timeout`、分层诊断（网关→公网 IP→DNS→HTTP，代理隧道断掉时前两层全通），以及⛔ **挂死期间的数据不可信**——慢会把两侧采样时刻拉开到跨过状态边界，它是一台残差制造机
- **v0.1.17（已合并）**：**一次刻意的无人介入实测**的回流——在一个低难度 A 类目标（日文企业周年微站，GSAP+ScrollTrigger+Swiper 静态站）上让 skill 从 Step 0 跑到 M(n)，全程不介入，如实记录卡在哪。**跑不到头**：12 处摩擦，其中 4 处必须改脚本才能继续、3 处是"纪律有整节文档而仪器根本不存在"、1 处是上一个项目里**正在跑的假绿**。全部修完并在该目标上回归验证。① ⭐⭐ **检查者不能是生产者**（`verification-gates.md` 新增 §2.1.2）：外壳字节门用 `import { PAGES } from "./build-site.mjs"` 拿页面清单，而构建逻辑在顶层执行——**门在审计产物之前先把产物重新生成了一遍**。实测：往产出里注入一个字节，门报 **PASS 0** 且那个字节**跑完就没了**；而它的收尾语正是"未解释的字节就是未登记的编辑"。更深的一半是断言变成**循环论证**（产物由变换表生成、再拿变换表解释产物）。它能活很久是因为**日常表现与正确的门完全一样**，只在产物被构建之外的东西动过时失效——而那正是它要防的事。纪律三条，含"每道门配一次注入 fixture：必须报红，且跑完那个字节还在"。② **判定树判不了最典型的 A 类站**：第 4 条把 three 强签名写成 A 的**必要条件**，于是纯 GSAP 滚动站（three=0，其余四条全中）落不进 A，而 B 的附加条件清单一条都对不上；改为条件式（有 3D 才要求强签名），并补"D 信号在**目标路径**上量、不在宿主域上量"（企业 CMS 域下的静态微站，宿主 `wp-admin` 不是目标的 D 信号）。③ **补齐三件缺失的仪器**：`verify-offline.mjs`（§1.6 讲了整节的枚举式零外联门，此前只存在于某个项目里）、`build-site.mjs` + `verify-shell.mjs`（策略 A 的构建层与外壳门，通用骨架 + 站点自带 `shell-config.mjs`；两者都只 import 那份**无副作用**配置，结构上杜绝 §2.1.2 那个陷阱）、`serve.mjs --fallback-root`（"镜像是唯一资产库、不复制"这条主推策略此前没有伺服机制）。④ **四条健壮性**，共同点是**失败形态都是静默的**：`netcapture` 被源站一个畸形 URL（**主机名里 percent-encoded 空格**，GTM 运行时拼出来的）整轮打死→改为容错并单列报告（页面拼出坏 URL 本身是关于源程序的发现）；`serve` **静默忽略未知旗标**→响亮失败（实测一次静默忽略造成首页 121 个 404）；`fingerprint` 把图例印成结论（`sourceMappingURL = 0` 后面照印"有公开 sourcemap"，`= 1` 那次指向的是被拼进来的库残留、实测 `.map` **404**）→只印适用分支 + 打印 URL + **实拉 `.map` 验活**；`extract-source --balance-check` **先剥脚手架再解析**、于是看不见脚手架自己造成的错误（生成的 `export` 让经典脚本抛 `SyntaxError`、整页一行没跑）→命中即告警。⑤ `pixelcompare --self`：§1.3.2 要求的自比带宽**按定义是一侧与自己比**，会被跨侧假绿守卫拦下——给一条合法通道，产物标 `kind:"self-band"`，且 `--max-mean` 对它失效（带宽是分类的**输入**，不是判决）
- **v0.1.18（已合并）**：**第二次无人介入实测**（换形态：Nuxt 2 SSG + three.js/Pixi/GSAP 命令式引擎 + Contentful 烘焙内容）的 M0 回流。⭐ **v0.1.17 那 12 条一条没复发**，而新形态又开出三条，**全在 M0、全是同一个形状——镜像报告自己抓完了，而它没有**。① **爬虫静默丢弃白名单外的主机引用**：本站作品图在自定义媒体域上，首轮报"57 files saved, 2 failed"（读起来像成功）而**丢掉 827 条引用**；netcapture 早有 off-host 普查与告警，爬虫一个字都没有。现补上，且**建议行只列看起来像资产主机的**（样本 URL 带扩展名）——否则会建议你去镜像 `www.w3.org`；筛完为空就不告警，**对着命名空间报警会训练读者忽略它**。② ⭐⭐ **引用提取器要求属性前缀，于是"JS 里的纯字符串根相对路径"整类不可见**——`new Workbox("/sw.js")` 匹配不上任何形状，而 **两遍镜像共享这个盲区**：浏览器在页面上下文之外取 service worker，CDP 抓包也看不见它（`sw.js` 在抓包账本里命中 0）。抓到它的是 CLEAN 探针的一句 404，**在下游两步之外**。新增形状 3b（不要求属性上下文，**仍要求文件扩展名**，否则每个路由字符串都变幽灵资产），在一份**刚刚 PASS 0** 的镜像上一次性开出 **17 条真实缺失资产、零误报**——其中包括 **`/displacement.png`，签名效果读的那张 WebGL 位移贴图**。③ **`--seeds` 补漏会截短账本**，而它的卖点正是"落进同一本账"：manifest 每轮从空重建，一次只碰部分文件的补漏跑丢掉上一轮全部行，实测 238/238 的镜像变成**磁盘 255 / 账本 224 / 31 个孤儿**（覆盖度门的报错文案甚至已经猜到了原因，说明这个失败模式此前被观察到过、只是没修）。改为**累积**：预载已有行但**不**标记为已抓，于是全量重爬仍会覆盖每一行、陈旧行不可能存活。⭐ **三条合起来是一条更大的观察**：`verify-mirror` 的断言问的是"账本与磁盘是否自洽"和"磁盘上的东西是不是你以为的那个"，**没有一项问"引用集本身完不完整"**——提取器有盲区时，闭包门会在一个残缺的引用集上诚实地报「= ∅」。这是 objectandarchive 的 N13（`.atom` 整类不可见）之后，同一条教训的第二、第三次发作
- **v0.1.19（已合并）**：第二次实测走完 M0.5→M(n-1) 的回流，四条，**全部围绕"序列化数据"这一层**——SSG payload 是一段把数据编码成程序的东西（参数去重、`/` 转义、**8.9× 压缩比**、占文档 54%），**字节层的工具在它面前集体失灵**。① ⭐⭐ **本地化认五种 URL 写法，payload 用的是第六种**：`https://host`（转义斜杠是为了让数据块里永不出现 `</script>`）。10 个页面里 **541 条**指向媒体域的这种写法全部没被改写，而**运行时零外联门只看得见其中 1 条**——payload 里的 URL 要等页面渲染到那一块才发出请求。**540 条潜伏外联藏在一次绿探针后面**；§1.6 那句"零外联是关于一份具名端点清单的声明，不是一次绿探针"从 v0.1.1 就写着，这次给了它一个 **540:1** 的数字。（第一版修复把 `/` 直接写进正则，被引擎当成 unicode 转义解析成 `/`，**匹配了真斜杠、一条没改、而且是静默的**。）② ⭐ **URL 本地化有两份实现且已漂移**（`serve.mjs` 响应时 / `lib/shell-build.mjs` 构建时，正是 §2.1.1 禁止的）：同一输入下镜像留 `href="http://host"`、构建产出 **`href=""`**——空 href 不是"首页"是"当前页"，一个静默坏链接。**外壳字节门全程绿**（两侧各自自洽），抓到它的是**新增的 payload 门**。修完仍不一致，因为两侧**启动参数**不同（`--origin-host` 少一个 host）——**"两侧被同样地变换"不只是代码一致，还包括参数一致，而没有任何门检查这一点**。③ **新仪器 `verify-payload.mjs`**：求值展开 payload 再按**叶路径**结构对拍（Nuxt2/Nuxt3 两种形态），并枚举 payload 内残留的绝对 URL。④ **noindex 注入锚在字面量 `<head>`，而 Nuxt 输出 `<head >`**——10 个页面一次都没注入，产出既无 noindex 也无非官方声明；**抓到它的是逐条命中下限**（`fired 0x, floor 10`），而这正是 `legal-and-deploy.md` 要求必须被门盯着的那条变换。配套：`verify-shell` 的纯插入特判改为**通用规则**（剥前缀再重放），此前按锚定方式写了三个具体特判、每换一种就红一次
- **v0.1.20（已合并）**：第三次实测（1.25 MB 单 bundle + 内联 three.js r158 + 自研命令式引擎，`gsap` 计数为 0）的回流。这一轮**专门去压移植层**，因为前两轮 19 条摩擦里 **10 条在 M0、0 条在移植层**。① ⭐⭐ **源程序按自己的域名分支，而镜像不在那个域名上**：`window.location.hostname=="lusion.co" && (CDN_PATH="https://lusion.dev")`——服务层把那个字面量**正确**改写了，但赋值根本不执行，`CDN_PATH` 保持空串，全部资产打到源站根路径，**首屏探针 78 个问题，而本地化层做得完全正确**。⭐ **本地化能改写字面量，改写不了"判断这些字面量该不该用"的条件**——这是它触及不到的一整类。`serve.mjs` 新增 `--rewrite FROM::TO`（登记式、**首次命中打印**，沉默此前与生效无法区分）。⚠ 更忠实的方案（Chrome `--host-resolver-rules`，一个字节源程序都不改）被一个平淡理由挡住：脚本用 Node `fetch` 校验服务器身份，**Node 不共享 Chrome 的解析规则**——理由已写进代码注释，作为下一步的入口。② **连续斜杠的归一化发生在 `path.join` 里而不是那份"唯一映射"里**：源站拼出 `…/textures//x.png`，爬虫落盘静默归一、`localRelPath` 保留双斜杠，账本与映射各说各话、serve 404 在一个明明在盘上的文件上。归一化移进共享映射——**在下游归一化正是四个消费者当初漂开的原因**。③ **扩展名判据 `{1,8}` 太短**，`.webmanifest` 有 11 字符→被当成页面写成目录，而 serve 按文件找→404；⭐ **`verify-mirror` 全绿**，因为账本与映射**共享同一个错误假设**——一致性门测不出被共享的错误前提（与 F14 同形）。④ ⭐ **本轮最值得记的是一个"没有摩擦"的结果**：M2 竖切（`.buf` 私有几何格式，切片取自 `_pretty` 行号、怪写法照抄、两本缺失清单写明）**一次成功**，数值门四项断言在 53 个文件 / 170,289 顶点上全绿，两条 fixture 精确命中。但要读对——它说明**在类名未混淆、格式自描述**时移植纪律够用，**没有**说明全混淆 bundle + 别名表的形态也够用。**三轮 22 条，移植层仍是 0，M0 仍是 11**
- **v0.1.21（已合并）**：第三次实测走完全量移植后的回流，两条，**都关于"门报了完美结果而画面是空的"**。① ⭐⭐ **像素门新增非空帧前置条件**：两张空帧对拍会报 `meanAbsDiff 0 / worstCellDiff 0 / 相似度 100`——**与完美收官同形**。实测：确定性冻结把该站引擎停在首帧之前，三条路由跨侧全报 0，而那些帧是 **201 色、99.5% 纯黑**（未冻结的对照帧 **28,282 色**）；更糟的是**冻结自比带宽也是 0**，我本来用它来证明冻结生效，而它对空帧同样满意。根因取证：冻结页 canvas 停在默认 `300×150`、常规页 `1728×1080`——引擎 init 从未执行，而 `__pump` 本身是好的（60 帧推进 1002ms），**这个引擎的启动序不是纯 rAF 驱动的**。skill 有 §4.8「全部的门拍在同一个状态里」、有「冻结的盲区」、有「有时正确的冻结是不冻」——**但没有一道门会问"这一帧上有东西吗"**。现在像素门开跑前先数颜色与主色占比，命中即 FATAL 退出；同一判据另出 `frame-census.mjs` 供事后复核任意截图。回归实测：冻结页当场被拦（97 色 / 99.3%），正常对拍照常放行（14,648 色 / 31.1%）。② `pixelcompare --pump dt,frames`：probe-shim 的头注写着"从 CDP 探针调用 `window.__pump`"，而这个脚本此前**没有任何入口**，只能把泵调用塞进 `--ready` 表达式——**纪律与仪器脱节的又一例**；现补为一等旗标，且 `window.__pump` 不存在时响亮失败而非静默跳过
- **v0.2+（剩余 B 类缺口，按需求频率排序）**：第三方存储桶/manifest 驱动资产发现、运行时 API 与 headless CMS 快照、Nuxt/Vue SSG payload 展开、多 chunk 大型 bundle 切片、Wayback 抢救流程
- **远期（C 类分支）**：声明式架构的"重构式逆向"方法论。实测显示大厂创意页正在向该形态漂移（Shopify Editions 三代 B→B→C），此分支迟早需要
- 每完成一个新的复刻项目，将其新经验合并回本 skill

## 许可

[MIT](LICENSE)。

**注意许可管的是这个 skill 本身，不管你拿它去复刻什么。** 复刻他人网站涉及的版权与合规判断由使用者自行承担——skill 内的授权前提（SKILL.md「使用前提与授权」）与本文件的"不能做"一节，才是关于**用途**的约束。

## 致谢

- 结构范式参考 [JimLiu/baoyu-skills](https://github.com/JimLiu/baoyu-skills) 的 baoyu-comic skill
- 方法论版权属于实践本身：六个源项目的 REBUILD_PLAN、engine-notes 与工具脚本是本 skill 的全部来源
