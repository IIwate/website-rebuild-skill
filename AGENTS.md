# 仓库维护准则

本文件适用于本仓库的维护与上游同步. 仓库提供基于客户端资产和运行记录的网站重建 skill, 维护工作以当前任务范围和已有授权为准.

## 仓库方向

- 以本地迭代方向为主吸收上游内容: 保留可追溯的来源, 明确工具边界, 用可复现行为核对实现与描述.
- 通过仓库或 skill 目录直接加载. 根 `package.json` 是私有开发包, 维护测试入口与本仓库元数据.
- 文档使用单一维护版本. npm 安装器, 发包配置, i18n 文档, CHANGELOG 和展示素材不在维护范围内; 临时采集物和测试产物不进入版本控制.
- 运行环境为 POSIX, 包括 Linux, macOS 和 WSL. Node.js 最低版本以 `package.json` 为准, 浏览器检查使用本地 Chrome 或 Chromium.
- 维护测试默认使用本地夹具和 loopback 服务. 仓库维护本身不构成对真实站点采集, 网站复刻或公开部署的授权.

## 文件职责

下文的 `scripts/`, `tools/`, `references/` 和 `assets/` 均指 `skills/website-rebuild/` 下的目录.

- [README.md](README.md): 仅记录当前与上游基线仍然存在的差异, 标明比较的上游提交. 不承载更新日志, 合并过程或测试流水账.
- [SKILL.md](skills/website-rebuild/SKILL.md): 使用范围, 授权边界, 核心流程和参考入口.
- [references/](skills/website-rebuild/references/): 按场景展开的机制, 约束和失败边界. `case-studies/` 保留真实项目的出处, 观察结果及适用条件.
- [scripts/](skills/website-rebuild/scripts/): CLI 与共享运行逻辑. 参数说明以脚本头注和 `--help` 为准, `scripts/README.md` 用于选择工具.
- [tools/](skills/website-rebuild/tools/): 使用开发依赖的源码整理工具. [assets/templates/](skills/website-rebuild/assets/templates/) 提供按项目选用的模板.
- [selftest/](selftest/): 离线与浏览器回归用例.

## 实现与文档

- 先读取现状再修改, 采用满足目标的最小正确改动. 优先复用现有实现和标准库, 避免新增重复工具, 推测性抽象, 兼容层或依赖.
- URL 映射, 查询策略, 清单格式, 内容协商, CDP 和 Chrome 生命周期优先使用 `scripts/lib/` 的既有实现. 构建层与服务层都处理的行为, 需要核对两侧实际输出.
- `scripts/` 使用 Node 内置模块和本地模块; 需要 AST 开发依赖的变换放在 `tools/`. 已有脚本通过子进程调用固定版本 npm 工具, 因此不能将无第三方 import 描述为无需外部工具或下载.
- 校验器可以复用无副作用的解析和格式约定, 但不能导入会重新生成待检查产物的入口. 共享实现的正确性仍需独立输入或反例支持.
- 保留输入验证, 防止数据丢失的错误处理和必要的失败退出码. 空输入, 解析失败, 未就绪或覆盖不足不能作为成功结果.
- 来源坐标和源码整理需要保持求值顺序, 共享闭包, 可变绑定及影响行为的随机消费顺序. token 相同不是所有运行行为等价的证明.
- 文档与注释只描述当前有效行为, 设计原因和限制. 使用中立专业表述, 不使用 emoji, 仪式化术语或无条件的完整性承诺. 历史细节仅放在确有解释价值的案例中.
- 用户回复用简体中文, 标点用英文半角. 代码注释与测试内容使用英文. Git 提交使用英文 Conventional Commits, 非平凡提交附简洁英文 bullets.

## 上游合并

1. 检查工作区, 分支, remotes 和共同祖先, 确认本地差异与上游新增范围. 尊重已有修改, 不覆盖无关内容.
2. 对照共同祖先审查双方改动. 除 Git 标出的冲突, 同时检查自动合并是否产生重复修复, 不同层的规则分歧, 错误的测试前提或文档与实现矛盾.
3. 以本地实现和维护方向为基线整合上游能力. 已有修复复用当前路径; 新能力需要同时适配共享逻辑, CLI, 文档与相关回归用例. 按内容取舍, 不整文件机械选择 ours 或 theirs.
4. 验证合并后的行为, 尤其是来源定位, 资源与清单一致性, 数据岛保护, 浏览器初始状态和错误判定. 测试阈值与覆盖范围不能为使合并通过而放宽.
5. 合并结果与必要验证确定后, 最后更新 README.md. 移除已被上游吸收的差异说明, 保留仍然有效的区别; 合并说明与提交消息承载本次操作记录.

## 验证

按改动涉及的风险选择检查, 不因流程固定而遍历全部测试.

| 命令 | 用途 |
|---|---|
| `npm test` | 共享逻辑, CLI, 本地 HTTP 夹具, 语法和文档引用 |
| `npm run test:browser` | Chrome, CDP, 像素比较, 抓包与导航行为 |
| `npm run version-gate` | 版本字段一致性 |
| `git diff --check` | 改动的空白与冲突标记检查 |

- 非平凡逻辑保留最小可运行回归, 断言实际输出或关键行为. 不用匹配源码写法, 提示语或测试数量代替行为证明.
- 修改文档时检查事实, 路径, 引用和格式. 不为纯文档修改运行无关的浏览器回归或添加镜像测试.
- 失败先定位到输入, 实现, 测试前提或环境条件, 修正后重跑受影响的检查. 已通过且未受后续修改影响的结果继续有效.
- 使用 npm 外部工具前确认固定版本可用. `npm_config_offline=true` 只控制 npm 获取行为; 要求完全离线时, 还需使用可用的网络隔离方式并保留 loopback.
- 版本修改同步根 `package.json`, `SKILL.md` 的 `metadata.version` 与 `scripts/lib/version.mjs`, 然后运行版本检查.
- 报告实际执行范围, 失败项和未验证部分. 分段验证按实际情况说明; 工具缺失或验证器不支持现有字段时, 区分工具限制与项目缺陷.

## Git 与执行边界

- 未获明确授权时, 不启动子代理, 不创建分支或 worktree, 不提交或推送. 不使用 reset 或 restore 丢弃已有修改.
- 同一任务和影响范围内的已有授权持续有效. 信息足够时直接推进, 只有关键缺口或权限限制才暂停受影响步骤.
- 外网访问遵守当前环境的代理与 `NO_PROXY` 约束. 代理不可用时停止依赖代理的步骤, 继续可独立完成的本地工作.
- 一次性命令设置合理超时. 长驻服务使用持久会话并保留输出, 验证结束后清理本次启动的服务与临时产物.
- WSL 下的依赖安装和构建使用 Linux 本地路径. 不在 WSL 删除宿主机挂载目录中的文件, 不跨 Windows/WSL 共享 `node_modules` 或二进制.

## Roadmap

以下按优先顺序安排后续维护. 具体实施范围由当前请求和复现证据确定, 每次任务无需遍历全部方向. 完成项归入实现及对应参考, 本节只保留后续工作.

### 1. 收敛本地化规则与数据岛处理

`serve.mjs` 与 `lib/shell-build.mjs` 仍各有 URL 本地化实现, `cfg.keepIslands` 的额外选择规则仅作用于构建配置. 优先对照主机与端口边界, 转义形式, 数据岛与载荷长度处理, 对已复现的不一致复用或收敛现有共享逻辑.

完成条件: 同一输入在需要一致的两层产生一致结果; 有意差异明确列出; 数据正文, 载荷长度和保留的外部资源仍可核对. 不以新的通用重写框架替代具体问题的修复.

### 2. 完善验证覆盖边界与失败诊断

从实际漏判或误判补充最小反例, 重点关注静态依赖与资源发现的未覆盖输入, 以及浏览器会话和媒体到达条件. 当前依赖检查只覆盖字面量 specifier, HTTP 冷缓存也不等于 cookie, localStorage 或 Service Worker 隔离. 先准确说明限制, 再为请求涉及的场景补观测或修复.

完成条件: 涉及非平凡逻辑的变更有可运行的成功与失败对照; 报告能够区分未检查, 前置条件不足和实际差异. 保留原件, 来源坐标与已有验收条件.

### 3. 持续精简文档并核对实际行为

随实现和上游变化核对入口, 参考, 案例, 模板与 CLI 说明. 将重复的参数说明集中到脚本用法, 给场景经验补足适用条件, 保留能解释机制与失败边界的真实案例.

完成条件: 入口能定位所需参考, 链接可解析, 描述与当前脚本一致; 案例观察与通用建议可区分; README.md 始终只反映当前与上游的差异.
